require "spec_helper"

describe Rubymagica do
  it "has a version number" do
    expect(Rubymagica::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
